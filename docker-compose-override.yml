services:
  api:
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: "http://+:8080"
    volumes:
      - ./src:/app/src  # Hot reload support
    
  sqlserver:
    environment:
      MSSQL_PID: Developer